#%PAM-1.0
# Tenodera gateway authentication — delegates to the Debian/Ubuntu system stack.
# (RHEL/Fedora use system-auth; that variant is shipped in the .rpm instead.)
auth       include    common-auth
account    include    common-account
